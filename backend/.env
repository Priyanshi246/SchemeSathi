PORT=5000
JWT_SECRET=schemesathi_secret_key_12345
# Enter your Google Gemini API key below to enable real generative responses.
# If left empty, the server automatically defaults to the high-fidelity offline rule simulation engine.
GEMINI_API_KEY=
# Enter your PostgreSQL Connection string below to connect to a live DB.
# If left empty, the server automatically defaults to the local file-backed JSON database schemesathi_db.json.
DATABASE_URL=
